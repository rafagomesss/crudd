<div class="row justify-content-center">
    <div class="col-sm-10 col-md-10 col-lg-6 col-xl-6">
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">EDITAR DESPESA</h6>
            </div>
            <div class="card-body">
                <form id="formExpenseEdit" name="formExpenseEdit" method="POST" action="">
                    <input type="hidden" name="id" value="<?= $this->expense->id;?>">
                    <input type="hidden" name="user_access_id" value="<?= \System\Session\Session::get('USER_ID');?>">
                    <div class="form-group">
                        <label for="category_id">Categoria</label>
                        <select class="form-control" name="category_id">
                            <option value="">Selecione...</option>
                            <?php if (is_array($this->categories) && !empty($this->categories)) : ?>
                                <?php foreach ($this->categories as $category) : ?>
                                    <option value="<?= $category->id;?>" <?= (int)$this->expense->category_id === (int)$category->id ? 'selected' : '';?>>
                                        <?= $category->description;?>
                                    </option>
                                <?php endforeach; ?>
                            <?php endif; ?>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="description">Descrição</label>
                        <input type="description" class="form-control" id="description" name="description" required data-validation="required" value="<?= $this->expense->description;?>">
                    </div>
                    <div class="form-group">
                        <label for="value">Valor</label>
                        <input type="value" class="form-control" id="value" name="value" required data-validation="required" value="<?= $this->expense->value;?>">
                    </div>
                    <button id="btnFormExpenseEdit" type="button" class="btn btn-primary float-right">Salvar</button>
                </form>
            </div>
        </div>
    </div>
</div>
